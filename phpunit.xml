<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>backend/app</directory>
        </include>
        <exclude>
            <!-- WP REST/AJAX router wiring: the register() path extends WP_REST_Controller and
                 needs a live WordPress REST stack, so it is covered by the e2e suite, not unit tests. -->
            <file>backend/app/Providers/HookProvider.php</file>
        </exclude>
    </source>
</phpunit>
